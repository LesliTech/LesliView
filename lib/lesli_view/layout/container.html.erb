
<%
    container_class = class_names(
        "lesli-application-container block w-full mx-auto",
        dashboard ? "container" : "max-w-7xl"
    )
%>

<turbo-frame
    id="<%= turbo_frame_id %>"
    class="<%= container_class %>">
    <%= content %>
</turbo-frame>
