
<%
    container_class = class_names(
        "lesli-application-container block w-full px-4 py-6 sm:px-6 sm:py-8 lg:px-8",
        dashboard ? "max-w-none" : "mx-auto max-w-7xl"
    )
%>

<turbo-frame
    id="<%= turbo_frame_id %>"
    class="<%= container_class %>">
    <%= content %>
</turbo-frame>
